management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
#  server:
#    port: 8090
  endpoint:
    shutdown:
      enabled: true
    health:
      show-components: always
      #show-details: always
  endpoints:
    web:
      exposure:
        include: "*"

info:
  app:
    name: hello-actuator
    company: yh
server:
  port: 8090
