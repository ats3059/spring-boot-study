management:
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
#  server:
#    port: 8090
  endpoint:
    shutdown:
      enabled: true
    health:
      show-components: always
      #show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
  server:
    port: 9292

info:
  app:
    name: hello-actuator
    company: yh

server:
  port: 8090
  tomcat:
    mbeanregistry:
      enabled: true

logging:
  level:
    hello.controller: debug


